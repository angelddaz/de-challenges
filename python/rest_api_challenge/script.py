from github import Github

token = ''
g = Github(token)

import ipdb; ipdb.set_trace()
# import pandas
# loop through some kind of github iterable object, pulling data
# store the data in dataframe
# dataframe -> local csv
# local csv -> local postgres
